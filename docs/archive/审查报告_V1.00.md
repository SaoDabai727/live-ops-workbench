# 合并计划审查报告

> 审查对象：`合并计划_V1.00.md`
> 审查版本：V1.00
> 审查日期：2026-07-17
> 结论：**计划整体架构合理，推荐方案可行，但存在 8 项 P1（应修复）和 5 项 P2（建议完善），无一票否决级问题。**

---

## P1 — 应修复（遗漏/模糊/未设计）

### P1-1：数据流缺少所有错误路径（§7 仅覆盖 Happy Path）

**问题**：§7「数据流（生成日报）」只描述了 5 步正常路径，未覆盖任何异常：

| 异常场景 | 现状 | 应补充 |
|---------|------|--------|
| daping BrowserView 不存在（用户从未打开过该房间的「直播大屏」子页） | 未处理 → executeJavaScript 会抛异常 | 自动创建 daping 视图（后台），等待 `did-finish-load` 后再抓取；或提示"请先打开直播大屏" |
| daping 正在加载中（自动刷新 60s 间隔触发） | 未处理 → executeJavaScript 可能在空白页执行 | 检查 `webContents.isLoading()`，等待 `did-finish-load` 或设置超时重试（沿用 A 的 15s 超时） |
| executeJavaScript 超时（A 有 15s 超时兜底） | 未提及 | 保留 15s 超时，超时回传 `{_timeout:true}` |
| innerText 为空 / 页面为错误页 / 登录页 | 未提及 → 正则匹配结果全部为空 | 检查 innerText 长度，若 <100 字符或含「请登录」「扫码」等关键词则提示"页面未就绪或需要登录" |
| webContents 已销毁（视图被回收后销毁） | 未提及 | `try/catch` 包裹 `executeJavaScript`，失败则创建新视图并重试 |
| 正则未匹配到任何 KPI（`_extract_kpis` 返回空） | A 的 `format_report` 用 `<未获取>` 兜底，计划提及但未映射到错误提示 | 在 IPC 回传时附加 `matchCount` 字段；渲染进程区分「部分缺失」与「完全无法获取」 |

**原因**：原计划的数据流描述偏向理想路径。实际运行中，以上场景在直播过程中高频出现（尤其是自动刷新导致 daping 短暂不可用）。

**修复建议**：在 §7 增加「异常路径」小节，逐条列出上述场景的处理策略。

---

### P1-2：日报子页的渲染模型与 B 现有子页模型不兼容

**问题**：B 的所有子页（juliang/daping/privateMsg/doc）都通过 `windowManager.showView()` → `webviewFactory.showView()` 创建 **BrowserView** 来渲染。但计划新增的「直播日报」子页不需要加载 URL，它展示的是**渲染进程内的 HTML 面板**（文本框 + 复制/保存按钮）。

这导致两处处理分歧：
1. `windowManager.showView()` 当前对所有子页调用 `factory.showView()` 并 `mainWindow.addBrowserView()`。切换到 report 时必须**跳过此逻辑**。
2. 渲染进程侧：`renderer.js` 需要在 `state.currentSubPage === 'report'` 时切换到一个 `<div>` 面板，而非常规的 BrowserView 覆盖。

**原因**：B 的 `subPages` 模型隐式假设每个子页对应一个 URL→BrowserView。report 是一个「无 URL 子页」，需特殊处理。

**修复建议**：在 §3.2 / §4.3 中明确：`subPages.json` 增加 `kind` 字段（已有 `"kind":"report"`），`windowManager.showView()` 根据 `kind` 做分支（`kind !== "report"` 时走 BrowserView 流程，`kind === "report"` 时仅更新 state 并 pushState 给渲染进程）。渲染进程在收到 `state.currentSubPage === 'report'` 时隐藏 BrowserView 区，显示报告面板。同时在 §6 增加冲突点 C11 记录此设计。

---

### P1-3：用户画像（profile）抓取流程未设计

**问题**：A 有两个独立的抓取操作——
- **KPI 抓取**（`scrape()` → `DEFAULT_SCRAPE_JS`）：取整页 innerText，提取 GMV/退款/曝光等。
- **用户画像抓取**（`scrape_profile()` → `PROFILE_SCRAPE_JS`）：搜索「看播核心用户画像」容器，提取标签（如「男 57%」「31-35 岁 23%」），回填到 `profile_edit` 文本框。

在合并计划中，**KPI 抓取已在 §7 数据流中描述，但画像抓取仅有一行提及**（§3.4「主播名单/用户画像/直播时长并入房间配置」），未说明用户如何触发、从哪个视图抓取、结果如何使用。

关键问题：B 的 daping 页面（`compass.jinritemai.com/screen/live/talent?live_room_id={roomId}`）上是否同时包含 KPI 和「看播核心用户画像」区域？如果不包含，需要额外打开一个「人群」页面才能抓取画像。

**原因**：计划在设计时以 KPI 为主线，画像被视为配置项的附庸，但它有一个独立的抓取动作。

**修复建议**：增加两种方案供选择——
- **方案 a（推荐，需实测验证）**：KPI + 画像在一次抓取中完成（从同一页面的 innerText 中提取），不区分两个动作。
- **方案 b（回退方案）**：在「直播日报」子页增加「抓取用户画像」按钮，单独触发 PROFILE_SCRAPE_JS 的移植版（可能需要先导航到人群页再抓取）。

---

### P1-4：`selectors.json` 命名与内容不一致

**问题**：A 的原始 `douyin_dom_selectors` 是一组 **CSS 选择器**（如 `[class*='gmvWrap'] [class*='hugeNumber'] span`），用于 `build_scrape_js()` 生成按选择器提取的 JS。但实际上 A 从 v7 开始已经不再使用这些 CSS 选择器——`build_scrape_js` 总是返回 `DEFAULT_SCRAPE_JS`（整页 innerText），实际 KPI 提取靠 `translator.py` 中的**标签正则**（如 `re.search(r"直播间成交金额\s*[¥￥]?\s*([\d,]+\.?\d*\s*[万亿]?)")`）。

计划 §4.4 说 `selectors.json` 存储的是「标签正则」，但文件名叫 `selectors`，容易让人误以为是 CSS 选择器；A 的 README §大屏选择器覆盖 也示范的是 CSS 格式，可能导致未来维护者困惑。

**原因**：A 自身存在命名历史债（文件名 `dom_selectors.py`，但 v7 后不再用 DOM 选择器），计划原样继承了这个模糊性。

**修复建议**：将 `selectors.json` 改为 `kpiPatterns.json` 或 `scrapeRules.json`，内容直接定义为 `{ "gmv": "直播间成交金额\\s*...", "refund": "退款金额\\s*...", ... }`——即正则字符串映射表，从 `translator.py._extract_kpis()` 导出。与 CSS 选择器彻底切割。同时在 §4.4 中删除对 CSS 选择器的引用，直接说明是标签正则。

---

### P1-5：`dailyUrl` 字段与 `roomId` 模板 URL 的优先级未定义

**问题**：A 的 `daily_url` 是用户每天手动粘贴的直播间 URL（因为抖音大屏每天的 URL 不同）。B 的 daping URL 通过模板 `...?live_room_id={roomId}` 自动构造。统一房间模型同时包含两者（§4.2），但未说明谁是主、谁是覆盖。

实际场景：
- 如果用户同时有 `roomId`（B）和 `dailyUrl`（A），抓取时用哪个？
- `dailyUrl` 来自 A 的旧数据，迁入后若 `roomId` 也能生成 URL，dailyUrl 实际上变成了冗余负担。
- 但 `dailyUrl` 也可能是用户对本房间的**特定覆盖**（A 支持 config.json 中覆盖 `daily_url`），不可简单丢弃。

**原因**：计划把两个字段视为共存，未声明优先级逻辑。

**修复建议**：在 §4.2 统一房间模型中明确：`dailyUrl` 优先级高于模板 URL（即若 `dailyUrl` 非空，用它覆盖模板生成的结果；若为空，使用 `getDefaultUrl(roomId, 'daping')`）。迁移时，A 的 `daily_url` 写入 `dailyUrl` 字段作为初始覆盖。用户可在「直播间管理」中清除 `dailyUrl` 以恢复模板 URL。

---

### P1-6：配置迁移的房间匹配算法未定义

**问题**：§4.4 提到「按房间 label/name 相似度或索引映射合并」，但未给具体算法。实际涉及两个操作：

1. **config 合并**：A `rooms{燃热→{anchors, profile, duration, daily_url}}` 匹配 B `liveRooms[{id:live1, label:美的燃热}]`。
2. **历史文件迁移**：`history/燃热/YYYY-MM-DD.txt` → `reports/live1/YYYY-MM-DD.txt`，依赖第一步的映射结果。

如果匹配错误（如 label 不是简单的包含关系），用户数据可能张冠李戴。目前的 substring 匹配（"燃热" in "美的燃热"）对当前 3 房间勉强可用，但对未来新增房间或不规则命名很脆弱。

**原因**：计划依赖隐式假设（两项目房间按同序排列），未定义确定性的映射规则。

**修复建议**：
- **主策略**：按索引顺序匹配（A 的 `ROOM_NAMES` 顺序 ↔ B 的 `liveRooms` 数组顺序）。因两个项目均由同一用户维护，房间编排大概率一致。
- **辅助策略**：对 match 失败的房间，在首次启动时弹出一个小对话框（Electron 的 dialog.showMessageBox 或简易 HTML 页），列出 A 的房间名和 B 的 liveRoom label，让用户手动勾选映射关系。
- **兜底**：手动映射后再无匹配→提示用户该房间数据需手动录入。

---

### P1-7：配置缺少版本字段，无向前兼容能力

**问题**：统一后的配置文件（`rooms.json`、`selectors.json`/`kpiPatterns.json`）均未定义版本字段。合并计划是一次性迁移，但如果未来 `rooms.json` 新增字段（如增加房间标签、多平台支持等），旧版配置会因缺字段而需要再次迁移——但代码无法判断当前配置是哪个版本。

A 的问题：`config_manager.py` 的 `_migrate_if_needed()` 用 `"rooms" in cfg` 判断是否已迁移——这是特征检测，脆弱。B 没有版本概念。

**原因**：当前两个项目都没有严格的配置版本化机制，计划继承了这一缺失。

**修复建议**：每个 JSON 配置文件的顶层增加 `"version"` 字段。统一后 `config.js` 的 load 函数读取 version 并决定是否需要执行迁移到最新 schema 的逻辑。迁移函数按版本链式执行（v1→v2, v2→v3...），与 A 的 `_migrate_if_needed` 逻辑类似但改用版本号驱动而非特征检测。

---

### P1-8：daping 视图引用存储位置不够可靠

**问题**：§C10 说「在 `appState.pages[roomId].daping` 维护该房间 daping 视图引用」。但 `appState.pages` 当前只存 `{lastUrl, isLoggedIn}`，不存 BrowserView 引用。B 的 BrowserView 实例由 `webviewFactory` 管理（池/保活/预加载），不是持久挂在 appState 上的。

抓取时需要从 `webviewFactory` 获取 daping 视图，而不是从 appState。如果视图不存在（池已满被销毁、或从未创建），需要强制创建。

**原因**：计划假定的 `appState.pages[roomId].daping` 引用与 B 的实际视图管理机制不一致。B 中视图属于 `webviewFactory`，appState 只存导航状态。

**修复建议**：`reportManager.js` 不依赖 appState 获取视图，而是调用 `webviewFactory` 的接口（如 `factory.getViewForRoom(roomId, 'daping')` 或直接 `factory.createView(roomId, 'daping')`）。`webviewFactory` 需新增 `getOrCreateView(roomId, subPage)` 方法，确保返回一个已存在或新创建的 BrowserView（已加载 URL），并在回调中执行抓取。同时注意：创建后可能需 `setBounds` 到隐藏位置（避免干扰用户当前视图）。

---

## P2 — 建议完善（增强/健壮性）

### P2-1：`last_report` 概念未延续

A 在每次抓取后保存 `last_report_<房间>.txt`，下次切换房间时自动恢复到文本框。这提供了「上次的日报还在、不怕关闭」的体验。统一后的「直播日报」子页如果不持久化上次结果，用户每次进入都是空白面板，需重新抓取。

**建议**：在 `navigationState.json` 中增加 `lastReport` 字段（每房间），或在 `reports/` 下保留 `last_report_<roomId>.txt`。切换房间或重新打开应用时自动恢复到报告面板。

---

### P2-2：缺少「页面为登录页」的检测

A 的 `web_engine.py.is_login_page()` 检查 URL 是否含 `login/passport/auth`，用于在抓取前提醒用户。B 的 daping 使用分区 cookie，理论上登录态持久，但若 cookie 过期或页面重定向到登录页，抓取会返回登录页的 innerText（而非 KPI），正则匹配全部失败——用户看到 `<未获取>` 但不知道是登录问题。

**建议**：在 `reportGenerator.js` 的正则提取前，增加一个防护检查：若 innerText 包含「请登录」「扫码登录」「passport」等关键词，直接返回错误 `{loginRequired: true}` 而非空 KPI。

---

### P2-3：保存/复制操作的异常处理未提及

A 的「保存到历史」（`_on_save_history`）有 try/except 并显示 QMessageBox；「复制到剪贴板」直接设置剪贴板文本。移植到 Electron 后——
- `fs.writeFileSync` 可能因磁盘满/权限问题失败 → 需 `try/catch` 并通过 IPC 回传错误。
- `electron.clipboard.writeText()` 一般不会出错，但在 macOS 沙箱场景可能受限 → 检查返回值。

**建议**：在 `reportManager.js` 的 IPC handler 中统一 `try/catch` 包裹文件写入，返回 `{ok:bool, error?:string}` 给渲染进程展示提示。

---

### P2-4：报告面板 HTML 结构与 B 的 BrowserView 遮挡层冲突

B 的内容区完全由 BrowserView 覆盖（`setBounds` 从 sidebarWidth+x 到 toolbar+tabBar+y）。如果报告面板是渲染进程中的 `<div>`，它会被 BrowserView 遮挡而不可见。在切换到 report 子页时**必须隐藏或移除当前 BrowserView**，否则用户看不到报告面板。

**建议**：在 `windowManager.showView()` 的 report 分支中额外执行 `mainWindow.removeBrowserView(currentView)` 或调用 `factory.hideView()`，确保报告面板可见。切换离开 report 时再恢复 BrowserView。

---

### P2-5：A 的 `launch_chrome_debug.bat`（Playwright 降级）未在计划中提及

A 的 README 记录了当 QtWebEngine 被抖音识别时的降级方案（启动外部 Chrome→Playwright CDP 连接）。在 Electron 中，由于使用的是真实 Chromium，理论上不会触发 UA 封锁。但计划可标注：**降级方案在 Electron 下不再需要（较 A 提升）**，避免困惑。

---

## 审查结论

**合并逻辑正确且完整**——跨栈统一到 Electron 的决策有充分论据，模块映射覆盖了双方的全部代码，新增模块职责清晰。

**发现的遗漏集中在三个层面：**
1. **错误路径缺失**（P1-1）：数据流只覆盖理想路径，8 种常见异常未设计应对。
2. **边界情况未覆盖**（P1-2、P1-3、P1-8）：report 子页与 BrowserView 模型冲突、画像抓取独立流、视图引用不准确。
3. **设计决策模糊**（P1-4 至 P1-7）：命名/优先级/匹配算法/版本化未明确，给后续实现留有不确定性。

**建议：将以上 P1 全部补充到 `合并计划_V1.00.md` 的对应章节，版本号更新为 `V1.01`。** P2 可在执行过程中逐项落实。
